# Bundled fixtures provide the source data. This selects only the CRM adapter;
# wire product, support, billing, or another CRM in create-connectors.ts.
CRM_PROVIDER=fixture
TENANT_ID=demo-tenant
FIXTURE_TENANT_ID=demo-tenant
FIXTURE_PATH=./data/fixtures/accounts.json
FIXTURE_NOW=2026-08-17T09:00:00.000Z
MASTRA_DB_URL=file:./mastra.db
CUSTOMER_SUCCESS_CRON=0 9 * * 1
CUSTOMER_SUCCESS_TIMEZONE=UTC
MAX_ACCOUNT_CONCURRENCY=4

# OpenAI performs the assessment, planning, and outreach generation.
GENERATION_MODE=model
MODEL=openai/gpt-5-mini
# Used for connector-neutral per-account cost reporting in model mode.
MODEL_INPUT_COST_PER_MILLION=0
MODEL_OUTPUT_COST_PER_MILLION=0
OPENAI_API_KEY=your-api-key
ENABLE_OBSERVATIONAL_MEMORY=false
ENABLE_SEMANTIC_RECALL=false
EMBEDDING_MODEL=openai/text-embedding-3-small

# To use HubSpot for CRM reads and writes, set CRM_PROVIDER=hubspot above and
# provide a private-app token. Other sources remain fixture-backed by default.
HUBSPOT_PRIVATE_APP_TOKEN=
HUBSPOT_BASE_URL=https://api.hubapi.com
HUBSPOT_RENEWAL_PROPERTY=renewal_date

# Optional remote libSQL/Turso storage.
TURSO_AUTH_TOKEN=
